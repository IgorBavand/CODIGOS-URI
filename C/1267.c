#include<stdio.h>
int main(){
    int n = 0, d = 0;

    scanf("%d %d", &n,&d);

    while(n != 0 && d != 0){
        for(int i = ; i < d; i++){
            int vetor[]
        }
    }
    return 0;
}
